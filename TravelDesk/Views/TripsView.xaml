<UserControl x:Class="DirectorFamilyTravelDesk.Views.TripsView" xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"><Grid Margin="28"><Grid.RowDefinitions><RowDefinition Height="Auto"/><RowDefinition Height="Auto"/><RowDefinition Height="*"/></Grid.RowDefinitions>
<DockPanel><StackPanel><TextBlock Text="Trip Register" Style="{StaticResource SectionTitle}"/><TextBlock Text="Compact master list — one row per trip" Foreground="{StaticResource Muted}"/></StackPanel><StackPanel DockPanel.Dock="Right" Orientation="Horizontal"><Button Content="Edit" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Edit_Click"/><Button Content="Delete" Style="{StaticResource SecondaryButton}" Margin="0,0,8,0" Click="Delete_Click"/><Button Content="+ New Trip" Style="{StaticResource PrimaryButton}" Click="New_Click"/></StackPanel></DockPanel>
<Grid Grid.Row="1" Margin="0,16,0,12"><Grid.ColumnDefinitions><ColumnDefinition Width="2*"/><ColumnDefinition Width="220"/></Grid.ColumnDefinitions><TextBox x:Name="SearchBox" ToolTip="Search trip, route, traveller, PNR" TextChanged="Filter_Changed"/><ComboBox Grid.Column="1" x:Name="StatusFilter" Margin="10,0,0,0" SelectionChanged="Filter_Changed"/></Grid>
<DataGrid Grid.Row="2" x:Name="GridTrips" SelectionMode="Single"><DataGrid.Columns><DataGridTextColumn Header="Trip" Binding="{Binding TripCode}" Width="105"/><DataGridTextColumn Header="Departure" Binding="{Binding DepartureDate,StringFormat=dd-MMM-yy}" Width="95"/><DataGridTextColumn Header="Return" Binding="{Binding ReturnDate,StringFormat=dd-MMM-yy}" Width="95"/><DataGridTextColumn Header="Route" Binding="{Binding RouteLabel}" Width="120"/><DataGridTextColumn Header="Travellers" Binding="{Binding Travellers}" Width="*"/><DataGridTextColumn Header="Pax" Binding="{Binding Pax}" Width="55"/><DataGridTextColumn Header="Onward" Binding="{Binding OnwardFlight}" Width="230"/><DataGridTextColumn Header="PNR" Binding="{Binding OnwardPnr}" Width="85"/><DataGridTextColumn Header="Seat" Binding="{Binding OnwardSeats}" Width="80"/><DataGridTextColumn Header="Return" Binding="{Binding ReturnFlight}" Width="230"/><DataGridTextColumn Header="PNR" Binding="{Binding ReturnPnr}" Width="85"/><DataGridTextColumn Header="Seat" Binding="{Binding ReturnSeats}" Width="80"/><DataGridTextColumn Header="Transport" Binding="{Binding Transport}" Width="120"/><DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="140"/></DataGrid.Columns></DataGrid>
</Grid></UserControl>